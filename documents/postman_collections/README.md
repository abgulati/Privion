### Simply import these collections into a Postman Workspace. I recommend creating a dedicated Workspace, such as 'LARS-Privion-ALL'. These are `Collection v2.1` exports.

### It's also recommended to import the HomeAssistant APIs Collection in a separate, dedicated Workspace.